function cont(){

    const number = parseInt(prompt("Enter a number: "));

    if (number > 0) {
        console.log("The sign is-" +" " + number);
    }

    else if (number == 0) {
        console.log("The sign is-" +" " + number);
      }

      else {
        console.log("The sign is-" +" " + number);
   }
}cont();